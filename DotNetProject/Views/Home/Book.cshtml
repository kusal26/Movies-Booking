@model IEnumerable<DotNetProject.Models.ShowTiming>

@{
    ViewData["Title"] = "Book";
}
@*
<p>
    @{
        string previousMovieName = null; // Initialize the previous movie name variable
    }
@foreach (var item in Model) {

  @if (previousMovieName != item.Movies.Name)
    {
            <h3 style=" color: rgb(255 255 255)"> @Html.DisplayFor(modelItem => item.Movies.Name) </h3>
        previousMovieName = item.Movies.Name;
    }
}
    
</p>
<table class="table">
    <thead>
        <tr>
            <th style=" color: rgb(255 255 255)">

            </th>
            <th style=" color: rgb(255 255 255)">
                Today
            </th>
            <th style=" color: rgb(255 255 255)">
               Soon
            </th>
            <th style=" color: rgb(255 255 255)">
                @Html.DisplayNameFor(model => model.available_seats)
            </th>
           
            <th style=" color: rgb(255 255 255)">
                @Html.DisplayNameFor(model => model.MovieHall)
            </th>
           
        </tr>
    </thead>
    <tbody>

       
@foreach (var item in Model) {
        <tr>
                <td >
                    @foreach (var item1 in item.MovieHall.Timing)
                    {
                        @if (item1.show_datetime == DateTime.Today || item1.show_datetime > DateTime.Today)
                        {
                            <ul>
                                <li style=" color: rgb(255 255 255)">
                                    <a style=" color: rgb(255 255 255); border:solid green; text-decoration:none" class="btn btn-danger"  asp-action="Seat" asp-route-id="@item1.TimingId">Book</a>

                                </li>
                            </ul>
                        }


                    }
                 


                </td>
            <td>
                @foreach(var item1 in item.MovieHall.Timing)
                {
               @if(item1.show_datetime==DateTime.Today)
                        {
                            <ul>
                                <li style=" color: rgb(255 255 255)">
                                    <p > @item1.show_datetime </p>
                    </li>
                </ul>  
                        }
                      
                        
                    }
            </td>
                <td>
                    @foreach (var item1 in item.MovieHall.Timing)
                    {
                        @if (item1.show_datetime > DateTime.Today)
                        {
                            <ul>
                                <li style=" color: rgb(255 255 255)">
                                    @item1.show_datetime
                                </li>
                            </ul>
                        }


                    }
                </td>
            <td>
                    @foreach (var item1 in item.MovieHall.Timing)
                    {
                        @if (item1.show_datetime == DateTime.Today || item1.show_datetime > DateTime.Today)
                           {
                            <ul>
                                <li style=" color: rgb(255 255 255)">
                                @item1.available_seats
                            </li>
                        </ul>
                           }

                    }
            </td>
               
                <td style=" color: rgb(255 255 255)">
                @Html.DisplayFor(modelItem => item.MovieHall.HallName)
            </td>
           
        </tr>
}
    </tbody>
</table>


<div class="container">
    @foreach (var item in Model) {
    <div style=" color: rgb(255 255 255)">
        @Html.DisplayFor(modelItem => item.MovieHall.HallName)
            @foreach (var item1 in item.MovieHall.Timing)
            {
                <div style=" color: rgb(255 255 255)">
                    <ul>
                        <li style=" color: rgb(255 255 255)">
                            @item1.show_datetime
                        </li>
                    </ul>
                </div>

            }
        </div>
    }
</div>

    *@

    <style>
        .schedule {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
            border-top: 1px solid #ccc;
        }
    </style>


    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
            <h2 style=" color: rgb(255 255 255)">Hall Name</h2>
            </div>
            <div class="col-md-6">
                <div class="schedule">
                    <div class="text-center">10:00 AM</div>
                    <div class="text-center">2:00 PM</div>
                    <div class="text-center">5:00 PM</div>
                </div>
            </div>
        </div>
    </div>

